<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleCalc1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Ensure these color attributes are defined -->
        <item name="buttonStyle">@style/CustomButton</item>
    </style>

    <style name="Theme.SimpleCalc1" parent="Base.Theme.SimpleCalc1" />

    <!-- Define the custom button style -->
    <style name="CustomButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/button_background_color</item>
        <item name="android:textColor">@color/button_text_color</item>
    </style>
</resources>
